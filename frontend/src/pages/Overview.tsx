export function Overview() {
  return (
    <section className="card">
      <h1>Overview</h1>
      <p className="muted">
        Management platform skeleton (E0). Hierarchy, configuration, and the map arrive with later
        epics.
      </p>
    </section>
  );
}
